import UserHubSubTasksPage from './UserHubSubTasksPage.jsx';

/** Kissflow entry — user subtasks hub (myitems / pending / participated on Sub_Task_Process_A00). */
export default function UserHubSubTasksProject() {
  return <UserHubSubTasksPage useLayout={false} />;
}
