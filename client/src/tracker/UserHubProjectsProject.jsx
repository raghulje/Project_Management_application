import UserHubProjectsPage from './UserHubProjectsPage.jsx';

/** Kissflow entry — user projects hub (owner-scoped). */
export default function UserHubProjectsProject() {
  return <UserHubProjectsPage useLayout={false} />;
}
