import UserHubTasksPage from './UserHubTasksPage.jsx';

/** Kissflow entry — user tasks hub (myitems / pending / participated). */
export default function UserHubTasksProject() {
  return <UserHubTasksPage useLayout={false} />;
}
